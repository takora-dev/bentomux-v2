{
  "id": "{{id}}",
  "name": "{{name}}",
  "version": "{{version}}",
  "apiVersion": 1,
  "description": "{{description}}",
  "author": "{{author}}",
  "entry": "index.js",
  "icon": {
    "type": "builtin",
    "name": "board"
  },
  "contributes": {
    "commands": [
      {
        "id": "{{id}}.ping",
        "title": "{{name}}: Ping"
      }
    ],
    "services": [
      {
        "id": "{{id}}.worker",
        "title": "{{name}} worker"
      }
    ]
  }
}
