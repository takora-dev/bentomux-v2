{
  "id": "{{id}}",
  "name": "{{name}}",
  "version": "{{version}}",
  "apiVersion": 1,
  "description": "{{description}}",
  "author": "{{author}}",
  "entry": "index.js",
  "icon": {
    "type": "builtin",
    "name": "board"
  },
  "contributes": {
    "commands": [
      {
        "id": "{{id}}.hello",
        "title": "{{name}}: Hello"
      },
      {
        "id": "{{id}}.open",
        "title": "{{name}}: Open"
      }
    ],
    "topbar": [
      {
        "id": "{{id}}.topbar",
        "title": "{{name}}",
        "icon": {
          "type": "builtin",
          "name": "board"
        },
        "command": "{{id}}.open"
      }
    ]
  }
}
