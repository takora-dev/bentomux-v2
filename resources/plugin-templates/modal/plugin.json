{
  "id": "{{id}}",
  "name": "{{name}}",
  "version": "{{version}}",
  "apiVersion": 1,
  "description": "{{description}}",
  "author": "{{author}}",
  "entry": "index.js",
  "icon": {
    "type": "builtin",
    "name": "board"
  },
  "contributes": {
    "commands": [
      {
        "id": "{{id}}.open",
        "title": "{{name}}: Open dialog"
      }
    ],
    "modals": [
      {
        "id": "{{id}}.dialog",
        "title": "{{name}}"
      }
    ]
  }
}
