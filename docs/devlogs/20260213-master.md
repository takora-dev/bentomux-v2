# Tab Reorder and Workspace Grouping

**Branch:** `master`
**Tanggal:** 2026-02-13
**Command:** `prompt`
**Tiket:** —

## Ringkasan

Menambahkan drag-and-drop untuk mengubah urutan tab terminal/diff dan grouping visual berdasarkan workspace.

## Perubahan

| File | Status | Keterangan |
|------|--------|-----------|
| `src/src/views/tabs.ts` | UPDATE | Drag-and-drop reorder dan grouping tab berdasarkan workspace |
| `src/styles.css` | UPDATE | Style group workspace dan cursor drag |

## Detail Teknis

- **Arsitektur:** State session `ui.tabs` tetap menjadi sumber urutan; render mengelompokkan tab berdasarkan `workspaceId`.
- **DB Changes:** Tidak ada.
- **API Changes:** Tidak ada.
- **Dependencies:** Tidak ada.

## Testing

- **Test Results:** PASS — `npm run typecheck`, `npm run build`.
- **Manual Test:** Drag tab ke tab lain; pastikan tab workspace sama tampil dalam satu group.

## Deployment

- **Migration:** Tidak ada.
- **Environment Variables:** Tidak ada.
- **Config Changes:** Tidak ada.

## Catatan

Urutan tab session-only, belum dipersist ke storage. Group label memakai nama workspace dan fallback `Other`.
