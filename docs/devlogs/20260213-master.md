# Tab Reorder and Workspace Grouping

**Branch:** `master`
**Tanggal:** 2026-02-13
**Command:** `prompt`
**Tiket:** —

## Ringkasan

Menambahkan drag-and-drop untuk mengubah urutan tab terminal/diff dan grouping visual berdasarkan workspace. Urutan tab sekarang dipersist ke `bentomux.json`, sehingga reorder tetap ada setelah quit dan buka ulang.

## Perubahan

| File | Status | Keterangan |
|------|--------|-----------|
| `src/src/views/tabs.ts` | UPDATE | Drag-and-drop reorder, grouping tab, dan sinkronisasi urutan ke backend |
| `src/src/views/sidebar.ts` | UPDATE | Persist reorder pane setelah drag |
| `src/preload/bentomux.ts` | UPDATE | Tambah IPC `reorderTabs` |
| `src/shared/types.ts` | UPDATE | Tambah kontrak API reorder tab |
| `src-tauri/src/commands.rs` | UPDATE | Validasi permutation tab dan simpan urutan |
| `src-tauri/src/lib.rs` | UPDATE | Registrasi command Tauri baru |
| `docs/devlogs/20260213-master.md` | UPDATE | Catat persistence fix, clickable agent links, close terminal dari sidebar, repaint xterm, dan retry IPC startup Windows |

## Detail Teknis

- **Arsitektur:** Renderer mengirim urutan `TabRec.id`; backend memvalidasi exact permutation lalu memakai `patch_state`, yang atomic-save ke storage.
- **DB Changes:** Tidak ada.
- **API Changes:** Tambah command `tab_reorder` / `reorderTabs`.
- **Dependencies:** Memakai `@tauri-apps/plugin-opener` yang sudah tersedia; tidak ada dependency baru.

## Testing

- **Test Results:** PASS — `npm run typecheck`, `npm run build`. Repaint fix refits and refreshes visible xterm terminals after focus/visibility changes and ResizeObserver events.
- **Manual Test:** Reorder tab/pane, quit app, buka ulang; urutan harus tetap sama. Klik URL `http://` dan `https://` dari output agent; masing-masing harus terbuka di browser default. Hover row terminal di sidebar, klik `×`; pane harus tertutup tanpa memilih/drag row.

## Deployment

- **Migration:** Tidak ada.
- **Environment Variables:** Tidak ada.
- **Config Changes:** Tidak ada.

## Catatan

Workspace reorder sudah dipersist sebelumnya. Tab reorder sebelumnya hanya mengubah `ui.tabs` session-state; fix ini menghubungkannya ke persisted backend state.

Terminal kini memakai WebLinksAddon dengan Tauri opener callback. Hanya URL `http:` dan `https:` yang dibuka; scheme lain ditolak.

## Git commit diff readability — 2026-09-22

- `src/styles.css`: wrap long diff code lines instead of truncating them with ellipses; keep empty diff states centered and patch scrolling intact.
- Validation: `npm run build` PASS.

## Git panel diff long-token wrap — 2026-09-22

- `src/styles.css`: `.diff-line` grid kolom kode dari `1fr` ke `minmax(0,1fr)` — track `1fr` punya min-width `auto`, jadi token panjang tanpa spasi memaksa track melebar melebihi panel dan teks terpotong, `overflow-wrap:anywhere` dari fix sebelumnya tidak pernah aktif.
- Validation: `npm run build` PASS.

## Performance Phase 0–1

**Tanggal:** 2026-03-10

- Menambahkan `npm run measure:build` untuk mencatat waktu build, total renderer bytes, jumlah file, dan 10 chunk terbesar.
- Menambahkan Rust cache CI melalui `Swatinem/rust-cache@v2`.
- Menambahkan `[profile.dev]` incremental agar iterasi Tauri lokal tidak memakai konfigurasi release.
- Mengurangi lazy Shiki grammar langka; bahasa yang belum punya loader tetap tampil sebagai plain text.
- Baseline sebelumnya: renderer sekitar 3.1 MB dengan chunk `cpp` sekitar 797 kB.
- Setelah Phase 1: renderer terukur 1,817,034 bytes; chunk `cpp` dan grammar langka tidak lagi dihasilkan.
- Validasi PASS: `npm run typecheck`, `npm run build`, `npm run measure:build`, `npm run test:backend` (133 tests), `cargo check --manifest-path src-tauri/Cargo.toml`.

**Windows startup race fix — 2026-09-22**

- `src/preload/bentomux.ts`: retry initial `get_state` IPC up to four times with bounded backoff. WebView2 can dispatch first invoke while native startup is still settling; later commands remain unchanged.
- File `src/src/main.ts`, `src/src/views/sidebar.ts`, dan `src/src/views/terminal.ts` memiliki perubahan existing sebelum task ini; tidak disentuh.


Visible xterm canvases now refit and force-refresh after window focus/visibility changes. This covers macOS/WebView compositor cases where agent output remains live but text styling disappears until selection triggers repaint.

Sidebar terminal kini punya kontrol close per pane. Klik kiri tombol `×` menutup pane lewat flow `closeTerminalPane`; event dihentikan agar tidak ikut mengaktifkan atau memulai drag row.

**Windows boot race: `pty` state not managed — 2026-09-23**

- Gejala: kadang muncul `Bentomux boot error: state not managed for field 'pty' on command 'tab_restore'` saat app dibuka ulang di Windows.
- Akar masalah: Tauri membuat window + WebView2 di dalam `setup()` sebelum closure `.setup(...)` app berjalan, dan IPC-nya datang dari thread WebView2. `AppStateManager` sudah di-`manage()` di Builder (fix `7107ea8`), tapi `PtyManager` masih di-`manage()` di dalam setup — setelah `PtyManager::new()` yang menjalankan handshake ke pty host daemon (spawn + tunggu sampai 6 detik, plus `restart_host` 4 detik). Kalau daemon harus di-spawn ulang (habis update atau habis idle-exit) handshake lambat, dan `tab_restore` menang balapan. Retry `get_state` (`46ec8d6`) tidak menolong: `get_state` sudah pre-managed sehingga sukses instan, lalu `restoreTabs()` yang gagal.
- `src-tauri/src/pty.rs`: `PtyManager::new()` kini I/O-free (aman dipanggil di Builder) dan `start(app)` yang menjalankan handshake, dipanggil dari setup. Ditambah gate kesiapan (`ready` + `Condvar`): command yang datang di tengah handshake menunggu, bukan membaca manajer kosong. Gate dibuka juga saat handshake gagal supaya command melapor `PTY host not connected` alih-alih menggantung; ada timeout 45 detik sebagai pengaman. Ini juga menutup bahaya kedua: `restore_terms` yang jalan sebelum transport siap akan membaca daftar live kosong, mengganti semua tab dengan shell baru, dan meninggalkan pane daemon sebagai yatim.
- Field `app` menjadi `Arc<OnceLock<AppHandle>>` dan reader thread membacanya saat emit, supaya `pty:data` / `pty:exit` yang terjadi sebelum setup selesai tetap sampai ke renderer.
- `restart_host` memakai `shutdown_host_now()` (tanpa gate) karena dipanggil dari dalam handshake; `shutdown_host()` publik tetap menunggu gate.
- Tes: `test_commands_wait_for_the_ready_gate` memastikan `live_terms` memblokir selama gate tertutup dan selesai setelah dibuka.
- Validasi PASS: `cargo check`, `npm run test:backend` (204 tests), `npm run typecheck`, `npm run build`.

**Windows frameless titlebar — 2026-09-25**

- Gejala: di Windows masih ada caption bar native di atas titlebar aplikasi, jadi bar-nya dobel dan tidak menyatu dengan frame.
- Akar masalah: `titleBarStyle: "Overlay"` + `hiddenTitle` di `tauri.conf.json` **hanya berlaku macOS** — `title_bar_style()` di `tauri-runtime-wry` di-`#[cfg(target_os = "macos")]`, jadi di Windows caption bar tetap digambar `decorations: true`.
- `src-tauri/tauri.windows.conf.json` (baru): `decorations: false` untuk window `main`. Tauri mem-merge file platform otomatis (`read_platform()` jalan sebelum `--config`, jadi berlaku di `tauri:dev` maupun build CI). Karena merge-nya RFC-7396 dan **array diganti total, bukan deep-merge**, kedua entri window ditulis lengkap — kalau `approval-overlay` tidak ikut ditulis, window itu hilang.
- `src/index.html`: `#themeBtn` dibungkus `.win-controls` (grup ini sudah disiapkan CSS-nya sejak `c0fa333` tapi belum pernah dipakai) plus tombol minimize/maximize/close. Ikon maximize/restore dua-duanya ada di DOM dan ditukar CSS lewat class `maximized` yang sudah di-toggle `api.onMaximized`, jadi tidak ada state ikon baru dan ikon tetap benar untuk maximize dari OS.
- `src/styles.css`: `.win-controls` di-scope ke `.windows`; di macOS/Linux grup itu hanya berisi tombol tema dan tampilannya identik seperti sebelumnya. `.tbtn.tclose` (hover merah) yang tadinya mati sekarang dipakai tombol close.
- `src/src/main.ts`: tambah class `windows` di `<html>` (`/Win/.test(navigator.platform)`, pola sama seperti `plugin/loader.ts`), lalu wire ketiga tombol ke `api.minimize()` / `api.toggleMaximize()` / `api.close()` — ketiganya sudah ada di preload dan backend sejak awal, jadi tidak ada perubahan Rust.
- Tombol baru otomatis aman dari drag titlebar: `DRAG_SKIP` sudah mengecualikan semua `<button>`, jadi klik tombol tidak memicu `startDragging()` maupun double-click-maximize.
- Verifikasi config: `tauri_utils::config::parse::read_from(Target::Windows, …)` (library asli, bukan reimplementasi) mengembalikan `decorations=false` untuk `main`, `approval-overlay` utuh, dan `security.csp` tetap. Diff leaf terhadap config dasar: tepat satu — `app.windows[0].decorations: true -> false`.
- Verifikasi visual: render headless memakai CSS + markup asli menunjukkan macOS dan Linux **identik piksel-per-piksel** sebelum vs sesudah perubahan, dan glyph tombol maximize bertukar saat class `maximized` aktif.
- Validasi PASS: `npm run typecheck`, `npm run build`, `cargo check`.
- Catatan: Snap Layouts (flyout hover di tombol maximize) tidak tersedia karena window frameless — Win+Arrow dan drag-to-edge tetap jalan. Perlu tes manual di Windows untuk drag, resize tepi, snap, dan maximize tidak menutupi taskbar.
